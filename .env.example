# Copy to .env (never commit .env) and fill in YOUR ECE Hub Buddy Supabase
# project's values: Supabase Dashboard -> Project Settings -> API.
# Use the publishable/anon key ONLY. Never put a service-role key in client code.
VITE_SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR-PUBLISHABLE-ANON-KEY
